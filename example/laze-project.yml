# import default build rules and host context
import:
    - $laze/default

builder:
    name: host2
    parent: host

# define an application named "hello"
app:
    name: hello
    depends: foo
    sources:
        - hello.c

subdirs:
    - foo
